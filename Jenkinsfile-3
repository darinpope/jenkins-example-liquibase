pipeline {
  agent {
    docker { image 'liquibase/liquibase:latest' }
  }
  
  stages {
    stage('Status') {
      steps {
        sh '''
        
         liquibase status --changeLogFile=db.changelog-master.xml --logLevel=debug
         liquibase update --changeLogFile=db.changelog-master.xml  --logLevel=debug
        
        '''
      }
    }
   }
  post {
    always {
      cleanWs()
    }
  }
}
