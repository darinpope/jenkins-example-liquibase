pipeline {
  agent {
    docker { image 'liquibase/liquibase:latest' }
  }
  
  stages {
    stage('Status') {
      steps {
        sh 'liquibase status --changeLogFile=my_app-wrapper.xml'
      }
    }
   }
  post {
    always {
      cleanWs()
    }
  }
}
