pipeline {
  agent any
  stages {
    stage('Validate') {
      steps{
        bat 'liquibase validate'
      }
    }
    stage('Deploy') {
      steps{
        bat 'liquibase update'
        bat 'liquibase % BUILD_NUMBER%%'
      }
    }
  }
}
